encmodel:
  fn: ./module/mlp_new
  name: MLPEncoder

decmodel:
  fn: ./module/mlp_new
  name: MLPDecoder

modelargs:
  dim_d: 10
  dim_m: 16
  depth: 3
  hidden_dim: 260
  predictive: True
  transition_model: LS
  activation: tanh

nft_model: 
  fn: ./module/subgroup_nft
  name: SubgroupNFT

nftargs:
  orth_proj: True
